/** 图表初始化与加载 */

document.addEventListener('DOMContentLoaded', function() {
    // 动态加载图表数据 (可在具体页面覆盖)
    loadCharts();
});

function loadCharts() {
    // 基础图表加载逻辑，具体图表由页面 JS 初始化
    console.log('图表模块加载完成');
}
