# Project context

Read `AGENT.md` for development constraints and `README.md` for setup.
The project exposes a local STDIO MCP server and a Codex Skill. Web and project
CLI entrypoints have been removed. `config/skills` and `config/domains` contain
reusable analysis prompts/templates; the supported workflow is described in
`skills/video-analysis/SKILL.md`.
