# Project agent instructions

The supported product surface is the `skills/video-analysis` Skill and the
`video-analysis-mcp` STDIO server. The legacy Web UI and interactive CLI are
not supported entry points.

Read `AGENT.md` for the complete engineering, testing, login, cookie, and live
platform guidance. Keep changes inside the MCP/service/storage boundaries and
run the local test suite before committing.
